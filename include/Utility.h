#ifndef ACRUSH_UTILITY_H
#define ACRUSH_UTILITY_H

#include <iostream>
#include <fstream>

using namespace std;

// console logs with color
ostream& cout_red(const string &msg);

ostream& cout_green(const string &msg);

ostream& cout_blue(const string &msg);

ostream& cout_magenta(const string &msg);

ostream& cout_cyan(const string &msg);

// ToDo: helper function to print array, linked list, tree and graph etc.

#endif //ACRUSH_UTILITY_H
